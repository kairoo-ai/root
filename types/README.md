# types

Cross-cutting TypeScript types shared across the app. Reserved skeleton — no business logic yet.

Rule: colocate types next to the code that owns them by default; globalize a type here only when it is genuinely cross-cutting (used by 2+ unrelated areas).
