# config

Declarative product config layer (config-as-code). Single source of truth for
site metadata, routes, navigation, feature flags, pricing tiers, SEO defaults,
and typed env access.

Reserved skeleton — no business logic yet. Import via the `@/config` barrel.
